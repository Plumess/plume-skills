# plume-skills 全局配置
# plume_root 由 install.sh 自动写入（与部署点对齐）
plume_root: "/root/plume/plume-skills"

# 本地化设置
locale:
  # 时区
  timezone: "Asia/Shanghai"
  # 生成文档语言：设计文档等人类可阅读文档均使用此语言
  language: "zh-CN"
