---
name: context-keeper
description: "ALWAYS invoke this skill — instead of any native memory write — when user says 保存上下文/save context/保存/save/context save, or when you see [CONTEXT-RECOVERY] or [CONTEXT-SAVE-URGENT], or when completing a significant phase of work."
---

# Context Keeper v2

Saves and restores task state across compact events and work sessions.
Uses Claude's native jsonl as ground truth — maintains only an index layer (snapshots + CONTEXT-INDEX.md).

## Locale

Read `$PLUME_ROOT/config.yml` → `locale.timezone` and `locale.language`.

> **Once a save signal fires, you MUST save — regardless of how little content there is.**
> Once a recovery signal fires, you MUST restore from files — do not rely on post-compact memory.

## Detect Mode

- `[CONTEXT-SAVE-URGENT marker-id=<mid>]` → **SAVE** (PreCompact blocked compact; full context — highest quality)
- `[CONTEXT-RECOVERY marker-id=<mid>]` → **SAVE then RESTORE** (compact executed; capture fallback, then restore)
- User request → **SAVE**
- User cleanup request → **CLEANUP**

## Storage Layout

```
~/.claude/projects/<slug>/
├── *.jsonl                      # Claude native — ground truth
├── memory/MEMORY.md             # Claude native — read-only for us
├── plume-context/
│   ├── CONTEXT-INDEX.md         # Full-history timeline index
│   └── sessions/<sid>-<marker-id>.md          # Primary snapshot
│                <sid>-<marker-id>-fallback.md  # Fallback (compact recovery)

$PLUME_ROOT/data/
├── .save-pending-<sid>-<marker-id>   # Marker file (phase=save|recovery)
├── journal/                     # digest daily reports
├── digest-hint/                 # daily digest trigger markers
└── reports/                     # digest research reports
```

**Slug**: `pwd | sed 's|/|-|g'` (Claude-native, keeps leading dash).
**Session ID**: first 8 chars of current session's jsonl UUID.
**Marker-id**: YYYYMMDD-HHMM timestamp (configured timezone), generated by PreCompact hook. Encoded in marker filename, NOT derived from current clock.
**MEMORY.md**: read during RESTORE for context, never written by us.

---

## SAVE Mode

### Steps

**Step 1 — Compute paths and ensure dirs**
```bash
SLUG="$(pwd | sed 's|/|-|g')"
SESSIONS_DIR="$HOME/.claude/projects/$SLUG/plume-context/sessions"
mkdir -p "$SESSIONS_DIR"
```

**Step 2 — Determine filename**

> **Key mechanism**: The signal text `[CONTEXT-SAVE-URGENT marker-id=<mid>]` carries the marker-id directly — no file read needed. The marker-id is the YYYYMMDD-HHMM timestamp generated by PreCompact, anchored in the marker filename. Primary and fallback always share the same marker-id.

- For `[CONTEXT-SAVE-URGENT marker-id=<mid>]` (primary save):
  - Extract `MARKER_ID` from signal text
  - Filename: `<session-id>-<MARKER_ID>.md`
- For user request (manual save, no signal):
  - No marker → `MARKER_ID=$(TZ=$TZ_NAME date +%Y%m%d-%H%M)`
  - Filename: `<session-id>-<MARKER_ID>.md`
- For `[CONTEXT-RECOVERY marker-id=<mid>]` (fallback after compact):
  - Extract `MARKER_ID` from signal text
  - Check if primary exists: `ls "$SESSIONS_DIR/<session-id>-$MARKER_ID.md" 2>/dev/null`
  - **If primary exists → skip writing fallback entirely, go directly to RESTORE**
  - If primary missing → filename: `<session-id>-<MARKER_ID>-fallback.md`

**Step 3 — Write snapshot** following `$PLUME_ROOT/templates/session-snapshot.md` template. Summarize from current context, not from reading jsonl.

**Step 4 — Rebuild CONTEXT-INDEX.md** following `$PLUME_ROOT/templates/context-index.md` template. Read all snapshots in `sessions/`, build cumulative index.

**Step 5 — Clear markers and confirm**
```bash
rm -f "$PLUME_ROOT/data/.save-pending-${SESSION_ID}-"*
```
Tell user: "Context saved — snapshot `<filename>`, [N] snapshots total."

---

## RESTORE Mode

### Steps

**Step 1 — Read CONTEXT-INDEX.md** from `~/.claude/projects/<slug>/plume-context/`
- Found → Step 2
- Not found → list `sessions/`, read snapshots, synthesize index
- Empty → report "no history available"

**Step 2 — Present** brief summary (2-3 lines): task in progress, next step, snapshot count.

**Step 3 — Load** snapshots: for each marker-id group, prefer primary over fallback (skip fallback if primary exists). Load 2-3 most recent marker-ids. Also read `memory/MEMORY.md` if exists (supplementary).

**Step 4 — Resume** the Next Step. Do not wait for user to re-explain.

**Step 5 — Uncertain about prior work?** Use Recovery Hints from the snapshot to grep jsonl — do NOT read the full jsonl file (context cost):
```bash
# Find the jsonl for this session
ls ~/.claude/projects/$SLUG/*.jsonl
# Grep for a specific keyword
grep -i "<keyword>" ~/.claude/projects/$SLUG/<session>.jsonl | tail -20
```
Only grep for specific keywords when genuinely uncertain. Show the user what you found and why.

---

## CLEANUP Mode

Manages snapshot and session data size. Triggered by user request.

**Step 1 — Scan** all `~/.claude/projects/*/plume-context/` dirs + associated jsonl. Compute per-project size and last-modified time.

**Step 2 — Check threshold** from `config.yml` → `context.max_data_size_mb` (default 500). Below threshold → report size and stop.

**Step 3 — Build candidates and present** following `$PLUME_ROOT/templates/cleanup-report.md` template. Two sources merged (staleness priority): >30 days inactive + top 10 by size.

**Step 4 — Execute** user's choice. Delete `plume-context/` for selected items. Ask separately about jsonl deletion. Never delete `memory/MEMORY.md`.

---

## Failure Modes

| Failure | Recovery |
|---------|----------|
| CONTEXT-INDEX.md missing or corrupted | Rebuild from available snapshots |
| sessions/ empty | SAVE: proceed normally; RESTORE: report "no history" |
| Disk write fails | Retry once; if still fails, output snapshot to chat |
| PLUME_ROOT unset | Warn user to run `install.sh --repair` |
